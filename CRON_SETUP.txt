# Configuração para backup automático com CRON
# Para ativar, execute: crontab -e
# E adicione a linha abaixo:
#
# CRON para backup a cada 2 horas:
# 0 */2 * * * cd /home/ermak/tcs-voleyball && bash backup.sh >> /home/ermak/tcs-voleyball/backup.log 2>&1
#
# CRON para backup diário às 22:00:
# 0 22 * * * cd /home/ermak/tcs-voleyball && bash backup.sh >> /home/ermak/tcs-voleyball/backup.log 2>&1
#
# CRON para backup a cada 6 horas:
# 0 */6 * * * cd /home/ermak/tcs-voleyball && bash backup.sh >> /home/ermak/tcs-voleyball/backup.log 2>&1
#
# Para mais informações, execute: man crontab
